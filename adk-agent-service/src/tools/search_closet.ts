/**
 * search_closet Tool (M4-6)
 * Cross-modal hybrid search over ES (closet + shared closet).
 */

export async function searchCloset(query: string, limit: number = 10): Promise<void> {
  throw new Error("Implement in M4-6: Closet search tool");
}
